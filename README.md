# WSL

このページをお気に入り登録しておく。

```cmd
wsl --install
shutdown /r /t 0
```

```cmd
wsl --install Ubuntu-22.04
```

# Raft

```sh
curl -fsSL https://raw.githubusercontent.com/YumaYX/raft/main/build.sh | sh
```

